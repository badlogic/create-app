{
  "name": "{{name}}-frontend",
  "version": "1.0.0",
  "description": "{{name}} frontend",
  "type": "module",
  "scripts": {
    "build": "vite build",
    "build:watch": "vite build --watch"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vite": "^5.4.10",
    "tailwindcss": "^4.0.0"
  }
}